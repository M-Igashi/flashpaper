{
  "name": "flashpaper",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "cargo install worker-build && worker-build --release"
  },
  "devDependencies": {
    "wrangler": "^4.54.0"
  }
}
